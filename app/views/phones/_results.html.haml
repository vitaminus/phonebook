.span7.search-result
  - if @phones.length == 0
    %h4 Phones not found by this name
  -else
    %table.table
      %thead
        %tr
          %th Name
          %th Phone
      - @phones.each do |phone|
        %tr
          %td= phone.name
          %td= phone.phone
    = will_paginate @phones, inner_window: 2, outer_window: 2, renderer: BootstrapPagination::Rails